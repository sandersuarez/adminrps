// Function that shows a message from the server
function showServerMessage(/*id, */message) {
    console.log(message);
    /*let container = $(id);

    // Si el container existe se muestra y actualiza su contenido
    if (container.length) {
        container.empty();
        container.append($('<p></p>').text(message));
        container.show();
    }*/
}